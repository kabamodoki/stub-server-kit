# .local.env.example
# このファイルをコピーして .local.env を作成し、自分の環境に合わせて編集してください。
# .local.env は Git にコミットしないでください（.gitignore に追加推奨）。

API_URL=http://localhost:3000
DB_HOST=localhost
DB_PORT=5432
DB_NAME=myapp_dev
DB_USER=postgres
DB_PASSWORD=password
NODE_ENV=development
